# Envs (secrets) — cada app tem os seus; nunca versionar
.env
.env.local
.env.*.local
apps/web/.env
apps/web/.env.local
apps/cms/.env

# Ferramentas locais
.mcp-ssh.lock

# Os .gitignore de cada app (apps/web, apps/cms) cobrem node_modules, dist, etc.
